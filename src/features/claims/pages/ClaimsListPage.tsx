export default function ClaimsListPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-slate-900">Claims</h1>
      <p className="mt-2 text-slate-500">Claims module — scaffold only. Follow the `quotes`/`policies` pattern.</p>
    </div>
  );
}
