// Public API for the underwriting feature — scaffolded, not yet implemented.
// Follow the pattern in src/features/quotes (RTK Query for server state;
// a Redux Toolkit slice, namespaced under its own reducer key, for any
// state — even feature-local UI state — since Redux Toolkit is the
// single state-management tool for this app).
export {};
