No `policiesSlice` exists yet. Before adding one, follow Rule Zero
(`.claude/rules/clarification-protocol.md`): confirm the API contract with
`policies/services` first, then add the slice and register it in
`src/redux/store.ts` in the same change as its consuming page.
