# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Button.ct.tsx >> is not clickable while loading
- Location: tests\ct\Button.ct.tsx:18:1

# Error details

```
Error: expect(locator).toBeDisabled() failed

Locator: locator('#root').locator('internal:control=component').getByRole('button')
Expected: disabled
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeDisabled" with timeout 5000ms
  - waiting for locator('#root').locator('internal:control=component').getByRole('button')

```

```yaml
- button "Saving" [disabled]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/experimental-ct-react';
  2  | import { Button } from '@/components/ui/Button/Button';
  3  | 
  4  | /**
  5  |  * Playwright CT: real-browser interaction test, distinct from the Vitest
  6  |  * unit test on the same component — see .claude/rules/testing.md.
  7  |  */
  8  | test('calls onClick when the user presses the button', async ({ mount }) => {
  9  |   let clicked = false;
  10 |   const component = await mount(
  11 |     <Button onClick={() => (clicked = true)}>Save changes</Button>,
  12 |   );
  13 | 
  14 |   await component.getByRole('button', { name: 'Save changes' }).click();
  15 |   expect(clicked).toBe(true);
  16 | });
  17 | 
  18 | test('is not clickable while loading', async ({ mount }) => {
  19 |   let clicked = false;
  20 |   const component = await mount(
  21 |     <Button isLoading onClick={() => (clicked = true)}>
  22 |       Saving
  23 |     </Button>,
  24 |   );
  25 | 
> 26 |   await expect(component.getByRole('button')).toBeDisabled();
     |                                               ^ Error: expect(locator).toBeDisabled() failed
  27 |   expect(clicked).toBe(false);
  28 | });
  29 | 
```